# Suppress audit exit code for known unfixable transitive vulnerabilities.
# All 4 moderate CVEs are inside next.js (postcss) and exceljs (uuid) —
# neither is in application code and neither has an upstream fix available yet.
# Revisit when next >16.3.0 and exceljs >4.4.0 are released.
audit-level=high
